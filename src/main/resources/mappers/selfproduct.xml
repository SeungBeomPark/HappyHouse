<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="selfproduct"> 
   
   <select id="selectList1" resultType="Selfproduct"> <!-- type 1 -->
         select no, p_name, content, price, type
         from selfproduct
         where type = 1
         order by no asc      
   </select>
   
   <select id="selectList2" resultType="Selfproduct"> <!-- type 2 -->
         select no, p_name, content, price, type
         from selfproduct
         where type = 2 
         order by no asc    
   </select>
   
   <select id="selectList3" resultType="Selfproduct"> <!-- type 3 -->
         select no, p_name, content, price, type
         from selfproduct
         where type = 3 
         order by no asc    
   </select>
  
   
</mapper>   